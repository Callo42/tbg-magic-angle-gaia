# Equivalence flags -- substrate-chern-flat-band-gaia

(discovery skipped -- narrow target supplied; no broad candidate pairs scanned)

Selected-evidence internal pass: no same-proposition duplicate was found among the two distinct `gcn_*` claims. Decomposition helpers are scoped downstream views of the root, not independent equivalent evidence.
