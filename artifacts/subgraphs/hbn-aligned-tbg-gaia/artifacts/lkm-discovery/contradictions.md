# Contradiction flags - hbn-aligned-tbg-gaia

(discovery skipped - narrow target supplied; no broad candidate pairs scanned)

Payload-level audit: no `contradiction(...)` operator was emitted. The selected chain contains modeling caveats and open review targets, but no two selected evidence claims assert mutually exclusive values for the same system under the same conditions.
