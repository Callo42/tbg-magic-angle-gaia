"""cross_paper -- cross-paper operators for the twisted monolayer-bilayer subgraph.

This standalone subgraph is rooted in one Nguyen et al. 2022 evidence chain.
No cross-paper Gaia operators are emitted because the selected evidence contains
only one source paper; parent-package bridge candidates are audit-only here.
"""

from gaia.lang import contradiction, equivalence, induction, support


# No cross-paper Gaia operators are emitted in this standalone subgraph.
