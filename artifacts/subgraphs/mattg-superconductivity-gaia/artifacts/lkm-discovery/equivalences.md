# Equivalence flags - mattg-superconductivity-gaia

Discovery skipped because a narrow root was supplied.

No independent same-proposition pair appears inside the selected single-chain payload. The decomposition helper claims are scoped subclaims of the selected root, not independent evidence, so they are linked with `support(...)` from the root rather than `equivalence(...)`.
