# Contradiction flags - mattg-superconductivity-gaia

Discovery skipped because a narrow root was supplied. No same-scope contradiction pair appears inside the selected single-paper evidence payload.

Open-problem audit from selected evidence:

| item | decision | package action |
|---|---|---|
| Non-self-consistent displacement-field model `Delta V=dD/epsilon_0` omits electronic screening and non-local screening corrections. | Modeling limitation and open quantitative review target, not a contradiction in the selected payload. | Retained as premise `gcn_71314760_mattg_displacement_field_modeling` with capped prior. |
| Continuum model neglects direct top-bottom layer coupling and high-order/non-local interlayer terms. | Modeling limitation that can affect bandwidths, particle-hole asymmetry, VHS energies, and hybridization strengths; no counterclaim in selected payload. | Retained as premise `gcn_c680c239_mattg_continuum_approximations` with capped prior. |
| `theta/sqrt(2)` mapping and `theta_MATTG` near 1.6 degrees depend on ideal mirror symmetry and neglected terms. | Approximate mapping with scoped validity, not a contradiction unless future evidence gives a same-system incompatible magic-angle value. | Retained as premise `gcn_3618ccee_mattg_theta_over_sqrt2_mapping` with capped prior. |
