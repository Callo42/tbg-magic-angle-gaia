# Merge audit -- twisted-few-layer-graphite-gaia

| pair | verdict | reason | dsl_action |
|---|---|---|---|
| `gcn_2fcfd55afc454c94` / `gcn_2ceab67f16f34d1e` | keep distinct | The first premise states rigid, uniformly twisted slab and no-relaxation modeling assumptions; the second premise states the minimal-model parameter scan and full-parameter sensitivity caveat. They are complementary assumptions, not duplicate propositions. | Separate `claim()` declarations used together as premises in `deduction(..., prior=0.95)`. |
| `gcn_f23eff9c755840f2` / `gcn_2ceab67f16f34d1e` | keep distinct | The root reports the principal 1.05 degree flat-band numerical correspondence to TBG within the continuum description; the premise scopes that estimate to the minimal parameter set and notes sensitivity to remote hoppings. The premise constrains the root rather than restating it. | Root remains the deduction conclusion; premise remains an independent leaf with a reviewer prior. |
| `gcn_f23eff9c755840f2` / `gcn_2fcfd55afc454c94` | keep distinct | The root concerns the numerical flat-band angle and TBG correspondence; the premise concerns structural modeling assumptions. | Root remains the deduction conclusion; premise remains an independent leaf with a reviewer prior. |
