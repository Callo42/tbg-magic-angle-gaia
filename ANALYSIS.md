# Analysis: TBG Magic Angle Gaia

## Scientific Throughline

The regenerated graph starts from one LKM-backed BM velocity root: `gcn_7bca73ad98eb4ed4`. Its connected extensions stay inside a TBG magic-angle mechanism program: continuum velocity suppression, electron-phonon inputs and resistivity scaling, phonon-mediated pairing calculations, Kohn-Sham and ab initio flat-band checks near 1.1 degrees, relaxation effects, BM magnetic-Bloch flat bands, and a conjectural merged-Dirac velocity-collapse mechanism.

No executable parent synthesis claims are used. The cross-paper file contains only `support(...)` edges between LKM-backed claims, with scope caveats recorded in `mapping_audit.md` and `merge_audit.md`.

## Stopping Point

The graph stops at 63 knowledge nodes rather than forcing growth to about 100. LKM searches found chain-backed candidates for pressure tuning, experimental superconducting domes, substrate topology, and multilayer or MATTG variants, but those branches did not have sufficiently direct chain-backed paths to the active BM/velocity/flat-band graph under the May 4 no-synthesis rule.

## Duplicate and Contradiction Audit

`gaia inquiry review --strict .` reports 0 possible duplicate claims, 0 warnings, and 0 errors. No `equivalence()` or `contradiction()` operator is emitted: accepted roots differ by method, parameter regime, magnetic flux, relaxation model, or conjectural mechanism rather than asserting same-scope incompatible propositions.

The 31 structural holes reported by strict review are internal generated helper claims (`__conjunction_result_*` / `__implication_result_*`), not science-facing source claims. They are logged in `merge_audit.md`.

## Current Graph Counts

`gaia compile .` reports 63 knowledge nodes, 19 strategies, and 0 operators. `gaia check --hole .` reports 0 holes across 22 independent claims. `gaia infer .` converges with exact JT inference and writes 44 beliefs. `docs/starmap.html` renders 51 starmap nodes and 54 edges.

## Risks

Several accepted roots are model-heavy and depend on continuum parameter choices, phonon-pairing assumptions, extrapolated geometries, DFT/ab initio approximations, relaxation protocols, Landau-level cutoff convergence, or conjectural continuity of merged Dirac points. Priors remain capped below 0.90 and marked `TODO:review`.
